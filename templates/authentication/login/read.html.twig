<!DOCTYPE html>
<html dir="ltr" lang="en" class="no-outlines">
<head>

    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- ==== Document Title ==== -->
    <title>{{ 'authentication.login.tab.title' | trans }}</title>

    <!-- ==== Document Meta ==== -->
    <meta name="author" content="">
    <meta name="description" content="">
    <meta name="keywords" content="">

    <!-- ==== Favicon ==== -->
    <link rel="icon" href="http://website.aradmag.com/wp-content/themes/shield/assets/image/matican/favicon.png"
          type="image/png">

    <!-- ==== Google Font ==== -->
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700%7CMontserrat:400,500">

    <!-- Stylesheets -->
    <link rel="stylesheet" href="{{ asset('assets/css/style.min.css') }}">

    <style>

        @font-face {
            font-family: 'AIranianSans';
            src: url('{{ asset('assets/fonts/arad-font/BRoya.eot') }}?#iefix') format('embedded-opentype'),
            url('{{ asset('assets/fonts/arad-font/BRoya.woff') }}') format('woff'),
            url('{{ asset('assets/fonts/arad-font/BRoya.ttf') }}') format('truetype');
            font-weight: normal;
            font-style: normal;
        }

        body, p, h1, h2, h3, h4, h5, h6, strong, td, tr, input {

            font-family: 'AIranianSans', sans-serif !important;
            font-size: medium !important;
        }

        .login-info {
            direction: rtl !important;
            text-align: justify;
            color: black;
            display: flex;
            flex-flow: column;
            padding: 25px !important;
            /*text-align: justify;*/
        }

        .login-info h2 {
            font-weight: bold;
        }

        .login-info h1 {
            font-weight: bold;
            font-size: 22px !important;
        }

        .highlighted {
            color: red;
        }

        .disabled {
            color: gray;
        }
    </style>

    <!-- Page Level Stylesheets -->

</head>
<body>

<!-- Wrapper Start -->
<div class="wrapper">
    <!-- Login Page Start -->
    <div class="m-account-w"
         data-bg-img="{{ asset('assets-2/images/pexels-photo.jpg') }}">

        <div class="m-account">
            <div class="row no-gutters">
                <div class="col-md-6">
                    <div class="m-account--form-w bg-light login-info">
                        <h1>راهنما</h1>
                        <p>برای ورد به صفحه اقساط کافیست شماره تلفن همراه خود را در کادر پایین وارد کرده و دکمه
                            ارسال رمز عبور را بزنید . یک کد 6 رقمی برای شماره شما پیامک می گردد . که آن کد 6 رقمی را در
                            کادر دوم ( کد تایید ) وارد کنید و دکمه ورود را کلیک فرمایید .
                        </p>
                        <p class="highlighted">
                            <strong >* نکته:</strong> اگر پیامک کد تایید به دستان نرسیده است ممکن است
                            پیامک تبلیغاتی سیم
                            کارت شما بسته باشد
                            .
                            میتوانید برای باز کردن پیامک های تبلیغاتی خود متن زیرا مطالعه بفرمایید.</p>
                        <p>برای دریافت کد ورود به سامانه اراد موبایل میبایست پیامک تبلیغاتی خود را فعال نمایید.</p>
                        <p>فعال سازی پیامک تبلیغاتی <strong>همراه اول</strong>
                            با ارسال پیامک عدد <strong>2</strong> به سامانه پیامکی <strong>8999</strong></p>
                        <p>فعال سازی پیامک تبلیغاتی <strong>ایرانسل</strong>
                            با ارسال پیامک عدد <strong>1</strong> به سامانه پیامکی <strong>5005</strong>
                        </p>
                        <h2 class="disabled">غیر فعال سازی</h2>
                        <p class="disabled">برای غیر فعال سازی پیامک تبلیغاتی همراه اول عدد 1 را به شماره 8999 ارسال
                            کنید؛ یا اینکه کد
                            دستوری 8999# یا 800# را شماره گیری نمایید.</p>
                        <p class="disabled">براي لغو دريافت پيام هاي تبليغاتي ارسال شده از طریق پنل اس ام اس ايرانسل عدد
                            2 را در متن
                            پيامک تبلیغاتی ثبت کنيد و به شماره 5005 ارسال کنيد</p>
                    </div>
                    {#<!-- Login Content Start -->#}
                    {#<div class="m-account--content-w  bg-transparent text-white">#}
                    {#<div class="m-account--content">#}
                    {#<h2 class="h2">{{ 'authentication.login.main.title'  | trans }}</h2>#}
                    {#<p>{{ 'authentication.login.main.description' | trans }}</p>#}
                    {#</div>#}
                    {#</div>#}
                    {#<!-- Login Content End -->#}
                </div>

                <div class="col-md-6">
                    <!-- Login Form Start -->
                    <div class="m-account--form-w bg-light">
                        <form class="m-account--form"
                              action="{{ path('authentication_login') }}{{ (reseller_token)? "?reseller_token=":"" }}{{ reseller_token }}"
                              method="post">
                            <!-- Logo Start -->
                            <div class="logo">
                                <img src="http://website.aradmag.com/wp-content/uploads/2019/12/Logo-site-22-100x100.png"
                                     alt="">
                            </div>
                            <!-- Logo End -->
                            <label class="m-account--title">{{ 'general.mobile.title'  | trans }}</label>
                            <div class="form-group">
                                <div class="input-group">
                                    <div class="input-group-prepend">
                                        <i class="fas fa-mobile"></i>
                                    </div>
                                    <input type="text" name="userMobile" placeholder="+989 xx xxx xx xx"
                                           class="form-control" value="{{ userModel.userMobile }}"
                                           autocomplete="off" required>
                                </div>
                            </div>
                            <div class="m-account--actions">
                                <input type="submit"
                                       class="btn btn-rounded btn-info"
                                       value="{{ 'general.send.password.title' | trans }}" name="password_button">
                            </div>

                            <hr class="bg-white">
                            <form action="#" class="mt-3">
                                <label class="m-account--title">
                                    {{ 'general.verify.code.title' | trans }}
                                </label>
                                <div class="form-group">
                                    <div class="input-group">
                                        <div class="input-group-prepend">
                                            <i class="fas fa-key"></i>
                                        </div>
                                        <input type="password" name="userPassword"
                                               placeholder="{{ 'general.password.title' | trans }}"
                                               class="form-control" autocomplete="off">
                                    </div>
                                </div>
                                <div class="m-account--actions">
                                    <input type="submit" class="btn btn-rounded btn-info"
                                           value="{{ 'general.login.title' | trans }}"
                                           name="login_button">
                                </div>

                                <!-- Page Level Scripts -->
                                <div class="message">
                                    <ul>
                                        {% for message in app.flashes('s') %}
                                            <li class="message-success">
                                                {{ message }}
                                            </li>
                                        {% endfor %}

                                        {% for message in app.flashes('f') %}
                                            <li class="message-failed">
                                                {{ message }}
                                            </li>
                                        {% endfor %}

                                        {% for message in app.flashes('w') %}
                                            <li class="message-warning">
                                                {{ message }}
                                            </li>
                                        {% endfor %}
                                    </ul>
                                </div>
                                <div class="m-account--footer">
                                    <p>&copy; {{ 'authentication.login.main.footer.description' | trans }}</p>
                                </div>
                            </form>
                        </form>
                    </div>
                    <!-- Login Form End -->
                </div>
            </div>
        </div>
    </div>
    <!-- Login Page End -->
</div>
<!-- Wrapper End -->

<!-- Scripts -->
<script src="{{ asset('assets/script/all.js') }}"></script>


</body>
</html>
